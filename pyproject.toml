[project]
name = "uuid-service"
version = "0.1.0"
description = "UUID generation microservice for AWS Lambda"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "boto3>=1.34.0",
  "pytest>=8.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
